#!/bin/bash
# Simplified init-panda for local development / CI
# Skips ATLAS CRIC/CA/schedconfig steps that require external network access.

mkdir -p /var/log/panda/pandacache /var/log/panda/jedilog
chmod -R 777 /var/log/panda || true
